package FrameWork;

public class SlingWindow {
    //todo:框架一 滑动窗口框架
// 滑动窗口的实际其实就是双指针
   /* void SlidingWindows( String s){
        //定义指针
        int left=0;
        int right=0;
        //根据窗口的不同去定义不同
        Object window=...;
        int res=0; //记录区间的大小
        //指针的移动
        while(right<s.length){
            //得到当前窗口指向的值并添加进窗口里面
            char c=s[right]
            //窗口的变化
            window.add(c)
            //指针移动
            right++;
            //进行窗口的一系列更新
            //左指针的变化,根据题目中一些条件去看窗口需要缩小
            while(left<right&& window needs shrink){
                //对于窗口的更新
                char d=s[left]
                window.remove(d)
                //缩小窗口
                left++;
                //进行窗口内数据的一系列更新
			...
            }
            //每次都是right-left+1得到该滑动窗口中的大小。
            res = Integer.max(res, right - left + 1);
        }
        return res;

    }*/
}
